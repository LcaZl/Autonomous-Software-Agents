- shared beliefset in communication is incosistent, i'll fix it.
- missing changes in 'Beliefs.js':
  	  - UpdatePlayers:
	    if(this.agent.multiagent)
		this.agent.communication.updatePlayerBeliefSet(player.id, fact)
	  - UpdateParcels:
	    if(this.agent.multiagent)
		this.agent.communication.updateParcelBeliefSet(parcel.id, fact)
	    
- updateParcels, add updateParcelBeliefSet(_,_,"remove") to all edge cases. 

----

update 27/1:

STILL TO FIX.
- added wd : working directory with tests for multi-agent communication and planning and edited files

  
  	     
